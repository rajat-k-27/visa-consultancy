// We'll use a simplified version without Three.js dependencies
// This component is no longer needed since we're using a CSS-based globe implementation

export function FloatingObjects() {
  // This is now an empty component - the floating elements have been moved to the CSS implementation
  return null;
}
